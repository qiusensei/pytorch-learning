# 05_resnet 依赖清单 (Python 3.11, conda env `pytorch`)
# 注意: torch 的 Apple Silicon (mps) 版本建议用 conda 安装, 不要用 pip 装 torch
#     conda install pytorch torchvision -c pytorch
torch==2.13.0
torchvision==0.28.0
numpy==2.4.6
matplotlib==3.11.1
pandas==3.0.5